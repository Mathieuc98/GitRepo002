git --version
git init
git config --global user.name "NAAAM"
git config --global user.email "EMAIL"
git config --global -e
git config --global core.autocrlf true
git status
git add demo00.txt
git add git\ comandos.txt
git commit -m "Initiele commit"
git add *
git commit -m "De 2de commit"
aanmaken .gitignore
git add *
git add .gitignore
git commit
git rm demo02.txt
git add git\ comandos.txt
git diff --staged
git log


FOLDER 2 -> github repo
Maak een 2de folder aan GitRepo01
git init
git config --global user.name "NAAAM"
git config --global user.email "EMAIL"
git config --global core.autocrlf true
git remote add origin https://github.com/UW_LOGIN/UW_REPO
git pull origin master
VOEG BESTAND TOE AAN master
git add *
git commit -m "Eerste locale commit"
MAAK EEN NIEUWE BRANCH (aftakking)
git branch ontwikkeling
git branch              (bekijk de branches die aanwezig zijn)
git checkout ontwikkeling   (selecteer branch ontwikkeling)
MAAK NIEUW BESTAND AAN
git add *
git commit -m "Eerste locale commit"
git commit -m "Nieuwe ontwikkeling is klaar"
git push origin ontwikkeling
LOGIN IN HET SCHERM !
MERGE de Ontwikkeling in de master
git checkout master
git merge ontwikkeling
git push origin master  (stuur de master naar de online repo)